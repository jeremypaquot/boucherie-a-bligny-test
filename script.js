const toggle=document.querySelector('.menu-toggle');const menu=document.querySelector('#menu');toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));menu.classList.toggle('open',!open)});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.classList.remove('open');toggle.setAttribute('aria-expanded','false')}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
function parisNow(){const parts=new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/Paris',weekday:'short',hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(new Date());const val=t=>parts.find(p=>p.type===t)?.value;return{day:['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].indexOf(val('weekday')),mins:Number(val('hour'))*60+Number(val('minute'))}}
const slots={1:[[420,720]],2:[[420,750],[840,1080]],3:[[420,750]],4:[[420,750],[840,1080]],5:[[420,750],[840,1140]],6:[[420,750]],0:[]};const now=parisNow();document.querySelector(`[data-day="${now.day}"]`)?.classList.add('today');const open=(slots[now.day]||[]).some(([a,b])=>now.mins>=a&&now.mins<b);const status=document.querySelector('#open-status');status.classList.toggle('open',open);status.querySelector('span').textContent=open?'Ouvert actuellement':'Fermé actuellement';
